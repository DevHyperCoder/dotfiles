#!/bin/sh

decrease () {
    pactl set-sink-volume 1 -$2%
}

increase () {
    pactl set-sink-volume 1 +$2%
}

vol () {
    echo `pacmd list-sinks | awk '/^\svolume:/{i++} i=='2'{print $5; exit}'`
}

"$1" "$@" 
